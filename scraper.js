const fs = require('fs-extra'); const path = require('path'); const axios = require('axios'); const cheerio = require('cheerio'); const UA = 'Mozilla/5.0'; const httpGet = async (url) => { const r = await axios.get(url, { headers: { 'User-Agent': UA }, timeout: 20000 }); return r.data; }; const loadConfig = function() { try { var cp = path.join(__dirname, 'config.json'); if (!fs.existsSync(cp)) return { sites: [], settings: { port: 3000, dataDir: 'data' } }; return fs.readJsonSync(cp); } catch (e) { return { sites: [], settings: { port: 3000, dataDir: 'data' } }; } }; const scrapeAllSites = async function() { console.log('scrapeAllSites called'); }; const scrapeGithubPagesSite = async function() { return { articles: [], totalSubscriptions: 0 }; }; const scrapeAirportNode = async function() { return { articles: [], totalSubscriptions: 0 }; }; const parseSubscriptions = function() { return []; }; module.exports = { scrapeAllSites, scrapeGithubPagesSite, scrapeAirportNode, parseSubscriptions, loadConfig };
